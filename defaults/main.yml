---
# defaults file for fubarhouse-rust

# Rust

rust_version: 1.20.0
rust_update: false
build_rust_from_source: false
rust_install_clean: false

# Cargo

cargo_items:
  - ripgrep

# Configuration

shell_profiles:
- .bash_profile
